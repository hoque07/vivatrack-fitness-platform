import CommunityPost from "../components/CommunityPost";
import DashboardCard from "../components/DashboardCard";
import Layout from "../components/Layout";
import PageHeader from "../components/PageHeader";
import { challenges, communityPosts } from "../data/community";
export default function Community(){return <Layout><PageHeader eyebrow="Community" title="Fitness accountability that keeps users engaged" description="A community feed can improve user retention through achievements, challenges, likes, comments, and follow interactions."/><section className="mx-auto grid max-w-7xl gap-6 px-4 pb-20 lg:grid-cols-[1.2fr_.8fr] lg:px-6"><div className="grid gap-5">{communityPosts.map(post=><CommunityPost key={post.id} post={post}/>)}</div><div className="grid content-start gap-5"><DashboardCard title="Community challenges"><div className="grid gap-4">{challenges.map(c=><div key={c.title} className="rounded-2xl bg-white/[0.06] p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-black">{c.title}</p><p className="mt-1 text-xs text-white/42">{c.members} members joined</p></div><span className="text-sm font-black text-viva-green">{c.progress}</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-viva-green to-viva-cyan" style={{width:c.progress}}/></div></div>)}</div></DashboardCard><DashboardCard title="Community business value" value="High retention" subtitle="Social features create habit loops"><p className="text-sm leading-7 text-white/55">Posts, challenges, public milestones, and coach comments can help VivaTrack become more than a tracker. It can become a fitness network.</p></DashboardCard></div></section></Layout>}
