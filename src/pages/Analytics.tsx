import { Area, AreaChart, Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import ChartCard from "../components/ChartCard";
import DashboardCard from "../components/DashboardCard";
import Layout from "../components/Layout";
import PageHeader from "../components/PageHeader";
import { bodyWeightData, consistencyData, exerciseProgress, personalRecords, volumeData } from "../data/progress";
import { compactNumber } from "../utils/format";
const tooltip={background:"#0a181d",border:"1px solid rgba(255,255,255,.12)",borderRadius:16,color:"white"};
export default function Analytics(){const completion=Math.round((consistencyData.filter(i=>i.completed).length/consistencyData.length)*100);return <Layout><PageHeader eyebrow="Progress Analytics" title="Turn training data into useful insight" description="Charts and performance tables show how a premium analytics layer can create upgrade value inside VivaTrack."/><section className="mx-auto max-w-7xl px-4 pb-20 lg:px-6"><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4">{personalRecords.map(r=><DashboardCard key={r.label} title={r.label} value={r.value} subtitle="Personal record card"/>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-2"><ChartCard title="Workout volume chart" description="Weekly increase in total training volume."><ResponsiveContainer width="100%" height="100%"><AreaChart data={volumeData}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.08)"/><XAxis dataKey="week" stroke="rgba(255,255,255,.5)"/><YAxis stroke="rgba(255,255,255,.5)" tickFormatter={(v)=>compactNumber(Number(v))}/><Tooltip contentStyle={tooltip}/><Area type="monotone" dataKey="volume" stroke="#47f07d" fill="#47f07d" fillOpacity={0.18} strokeWidth={3}/></AreaChart></ResponsiveContainer></ChartCard><ChartCard title="Body weight trend" description="Simple body weight tracking for goal-based planning."><ResponsiveContainer width="100%" height="100%"><LineChart data={bodyWeightData}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.08)"/><XAxis dataKey="week" stroke="rgba(255,255,255,.5)"/><YAxis stroke="rgba(255,255,255,.5)" domain={["dataMin - 1","dataMax + 1"]}/><Tooltip contentStyle={tooltip}/><Line type="monotone" dataKey="weight" stroke="#34d9ff" strokeWidth={3} dot={{r:5}}/></LineChart></ResponsiveContainer></ChartCard><ChartCard title="Weekly consistency chart" description="Workout completion by day."><ResponsiveContainer width="100%" height="100%"><BarChart data={consistencyData}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.08)"/><XAxis dataKey="day" stroke="rgba(255,255,255,.5)"/><YAxis stroke="rgba(255,255,255,.5)" domain={[0,1]}/><Tooltip contentStyle={tooltip}/><Bar dataKey="completed" fill="#a3ff4f" radius={[12,12,0,0]}/></BarChart></ResponsiveContainer></ChartCard><DashboardCard title="Completion percentage" value={`${completion}%`} subtitle="This week"><div className="mt-4 h-4 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-viva-green to-viva-cyan" style={{width:`${completion}%`}}/></div><p className="mt-4 text-sm leading-7 text-white/55">Completion percentage can become a premium report metric for coaching, habit tracking, and corporate wellness dashboards.</p></DashboardCard></div><div className="mt-6 rounded-[1.75rem] border border-white/10 bg-white/[0.055] p-6 backdrop-blur-xl"><h2 className="text-2xl font-black">Exercise progress table</h2><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="text-white/45"><tr><th className="border-b border-white/10 py-3">Exercise</th><th className="border-b border-white/10 py-3">Start</th><th className="border-b border-white/10 py-3">Current</th><th className="border-b border-white/10 py-3">Progress</th></tr></thead><tbody>{exerciseProgress.map(row=><tr key={row.exercise}><td className="border-b border-white/10 py-4 font-bold">{row.exercise}</td><td className="border-b border-white/10 py-4 text-white/65">{row.start}</td><td className="border-b border-white/10 py-4 text-white/65">{row.current}</td><td className="border-b border-white/10 py-4 font-black text-viva-green">{row.progress}</td></tr>)}</tbody></table></div></div></section></Layout>}
