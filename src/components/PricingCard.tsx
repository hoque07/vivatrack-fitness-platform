import { CheckCircle2 } from "lucide-react";
import type { PricingPlan } from "../data/pricing";
export default function PricingCard({plan}:{plan:PricingPlan}){return <div className={`relative rounded-[2rem] border p-6 backdrop-blur-xl ${plan.highlighted?"border-viva-green/50 bg-viva-green/10 shadow-glow":"border-white/10 bg-white/[0.055]"}`}>{plan.highlighted&&<span className="absolute right-5 top-5 rounded-full bg-viva-green px-3 py-1 text-xs font-black text-viva-bg">Most popular</span>}<h3 className="text-2xl font-black">{plan.name}</h3><p className="mt-3 text-sm leading-7 text-white/60">{plan.description}</p><div className="mt-7 flex items-end gap-2"><span className="text-5xl font-black">{plan.price}</span><span className="pb-2 text-sm text-white/45">/{plan.period}</span></div><button className={`mt-7 w-full rounded-full px-5 py-3 text-sm font-black transition ${plan.highlighted?"bg-viva-green text-viva-bg":"bg-white text-viva-bg hover:bg-viva-green"}`}>{plan.cta}</button><div className="mt-7 grid gap-3">{plan.features.map(f=><div key={f} className="flex items-start gap-3 text-sm text-white/75"><CheckCircle2 size={18} className="mt-0.5 shrink-0 text-viva-green"/><span>{f}</span></div>)}</div></div>}
