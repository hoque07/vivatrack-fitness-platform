export default function SectionHeader({eyebrow,title,description,centered=false}:{eyebrow?:string;title:string;description:string;centered?:boolean}){return <div className={`mb-10 ${centered?"mx-auto max-w-3xl text-center":"max-w-3xl"}`}>{eyebrow&&<p className="mb-4 text-sm font-black uppercase tracking-[0.32em] text-viva-green">{eyebrow}</p>}<h2 className="text-4xl font-black tracking-tight md:text-5xl">{title}</h2><p className="mt-5 text-base leading-8 text-white/60">{description}</p></div>}
